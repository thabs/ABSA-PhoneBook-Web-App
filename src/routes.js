//! Contacts
import {ContactsView} from 'features/contacts';

const routes = [
  //! Contacts
  {
    path: '/contacts/view',
    name: 'Contacts',
    component: ContactsView
  }
];

export default routes;
